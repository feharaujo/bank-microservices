spring:
  application:
    name: accounts
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8080

build:
  version: "1.0"

accounts:
  message: "Welcome to Bank account related APIs"
  contactDetails:
    name: "Felipe - Developer"
    email: "felipe@test.com"
  onCallSupport:
    - +310610001122
    - +310610001133